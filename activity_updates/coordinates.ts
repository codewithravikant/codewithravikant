// Coordinates - Spaceship Position Initialization
// Before plotting a course, set the spaceship's initial coordinates

// Declare and initialize mutable variables without explicit type annotations
// TypeScript will automatically infer the types based on the values

// X-coordinate of the spaceship
let x = 0;

// Y-coordinate of the spaceship  
let y = 0;

// Z-coordinate of the spaceship
let z = 0;

// Export variables for use in other modules
export { x, y, z }; 